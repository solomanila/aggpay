spring:
  application:
    name: gateway

  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        enabled: true
        namespace: public

server:
  port: 8080
  forward-headers-strategy: framework
