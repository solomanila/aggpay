spring:
  application:
    name: admin-service

  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        enabled: true
        namespace: public

server:
  port: 8085
